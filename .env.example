# Copy this file to .env and fill in your values.
# PORT - server port (default 1406)
PORT=1406

# NOTE: The game URL now lives in the FRONTEND env as REACT_APP_GAME_URL
# (see frontend/.env.example). If set, the Game page loads the game.
# If blank, the Game page shows "Coming Soon".
